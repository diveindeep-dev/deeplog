import React from 'react';
import Layout from '../layouts';
import Cover from '../components/Graphic/Cover';

const Note = () => {
  return (
    <Layout>
      <Cover text={`NOTE`} />
    </Layout>
  );
};

export default Note;
